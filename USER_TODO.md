# 같이 하는 설정 체크리스트 — Claude Code가 주도합니다

혼자 하는 단계는 없습니다. 킥오프 프롬프트를 넣으면 Claude Code가 아래 단계들을 순서대로 주도하고, 성준님의 손이 필요한 **딱 그 순간에만** "지금 이거 클릭해주세요"라고 요청합니다.

**성준님 몫은 4종류뿐:**
1. 관리자 권한(UAC) 창에서 "예" 클릭
2. 비밀번호·전화 인증번호·CAPTCHA 직접 입력 — ⚠️ 이것들은 AI에게 절대 알려주면 안 되는 것들이라 성준님만 할 수 있음
3. Steam 창작마당 "구독" 버튼 클릭 (3번)
4. 스트림 키를 OBS 설정창에 붙여넣기 (딱 1번, 채팅·파일엔 절대 입력 금지)

나머지 전부 — 다운로드, 압축 해제, 설치 실행, 설정 파일 작성, 페이지 열기, 완료 확인 — 는 Claude Code가 합니다.

---

## 단계별 역할 분담

### 1. 트위치 계정 (Phase 7 전)
- **Claude Code**: 가입 페이지를 열고, 화면 순서대로 뭘 클릭할지 실시간 안내. 2FA 설정 경로 안내. 완료 여부 확인.
- **성준님**: 아이디·비밀번호 입력, 전화 인증. (Claude Code가 이걸 대신 하겠다고 하면 그게 이상한 겁니다 — 거절이 정상)

### 2. VB-Cable 오디오 드라이버 (Phase 3 전)
- **Claude Code**: 설치 파일 다운로드 → 압축 해제 → 설치 프로그램 실행까지.
- **성준님**: UAC "예" 1번 클릭, 재부팅 승인.
- **Claude Code**: 재부팅 후 "CABLE Input/Output" 장치가 생겼는지 스크립트로 자동 확인.

### 3. veadotube mini 아바타 (Phase 4)
- **Claude Code**: 다운로드 → 실행 → 아바타 PNG 4장 미리 생성 → 앱 화면 기준으로 한 클릭씩 안내 (아바타 등록, 마이크를 "CABLE Output"으로).
- **성준님**: 안내대로 앱 안에서 클릭.
- **Claude Code**: 테스트 음성을 재생해서 입이 움직이는지 같이 확인.

### 4. Slay the Spire 모드 (Phase 1 전)
- **Claude Code**: ModTheSpire, BaseMod, Communication Mod 창작마당 페이지를 하나씩 열어줌.
- **성준님**: 각 페이지에서 "구독" 클릭 (총 3번).
- **Claude Code**: 다운로드 완료를 창작마당 폴더 검사로 자동 확인 → "Play With Mods" 실행 안내.

### 5. OBS (Phase 7)
- **Claude Code**: winget으로 설치 → **장면 구성(Scene Collection)과 프로필(QSV·720p30)을 설정 파일로 미리 만들어 넣어줌** → 성준님이 장면을 손으로 만들 필요 없음.
- **성준님**: UAC "예" 클릭, 그리고 Settings→Stream에 스트림 키 붙여넣기.
- **Claude Code**: 인코더·해상도 설정이 맞는지 검증.

### 6. 방송 전 최종 체크
- **Claude Code**: 업로드 속도 테스트 자동 실행(3~4Mbps 이상 확인), 10분 테스트 방송 동안 CPU·드랍 프레임 모니터링 후 리포트.
- **성준님**: 첫 방송 몇 회는 처음부터 끝까지 지켜보기 (safety.md — 필터 검증 전 무인 방송 금지).

---

# 킥오프 프롬프트 (복사해서 붙여넣기)

이 폴더에서 터미널 열고 `claude` 실행 후:

```
CLAUDE.md를 읽고 Phase 0부터 순서대로 진행해줘. USER_TODO.md의 단계들도
내가 알아서 하도록 기다리지 말고, 네가 주도해서 나와 함께 진행해 —
네가 할 수 있는 건 전부 직접 하고(다운로드, 설치 실행, 설정 파일 작성,
페이지 열기, 검증), 내 손이 필요한 순간(관리자 권한 클릭, 비밀번호나
인증번호 입력, Steam 구독 클릭, 스트림 키 입력)에만 정확히 뭘 어디서
클릭할지 알려줘. 각 Phase 끝나면 검증 결과 보여주고 내 확인 후 진행.
설명은 전부 한국어로, 초보 기준으로 해줘.
```

중간에 이어서 할 때:

```
CLAUDE.md 기준으로 현재 어느 Phase까지 완료됐는지 점검하고, 다음
Phase를 이어서 주도해줘. 내 손이 필요한 부분은 클릭 단위로 안내해줘.
```

문제가 생겼을 때:

```
방금 실패한 단계의 로그를 보여주고, 원인 후보 3개와 각각의 확인
방법을 제시해줘. 수정은 내 확인 후에 진행해.
```
